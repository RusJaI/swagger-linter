extends:
  - spectral:oas
rules:
  no-$ref-siblings: warn # error is the default value
  host-validation:
    given: $.host
    message: "{{property}} {{error}}"
    severity: error
    then:
      function: pattern
      functionOptions:
        match: "^[a-zA-Z:\\/\\.]*$"
